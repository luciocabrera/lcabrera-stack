/**
 * Same-origin resource route serving distinct filter options for
 * `transport: 'loader'` descriptors (the route's loader calls the BFF
 * server-side). Hard-coded app path precedent: PERSIST_COOKIE_ACTION.
 */
export const LOADER_FILTER_OPTIONS_PATH = '/_api/filter-options';

/** Path of the generic distinct endpoint under the BFF API base. */
export const BFF_DISTINCT_PATH = '/distinct';
