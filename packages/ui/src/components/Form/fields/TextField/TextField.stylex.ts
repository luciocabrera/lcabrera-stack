import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  textareaOverride: {
    paddingBlock: '0.5rem',
    resize: 'vertical',
    height: 'auto',
    minHeight: '5.5rem',
  },
});
