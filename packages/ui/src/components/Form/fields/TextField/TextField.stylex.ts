import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  textareaOverride: {
    height: 'auto',
    minHeight: '5.5rem',
    paddingBlock: '0.5rem',
    resize: 'vertical',
  },
});
