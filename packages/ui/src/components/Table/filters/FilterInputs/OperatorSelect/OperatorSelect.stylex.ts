import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  operatorOverride: {
    borderRadius: 0,
    overflow: 'visible',
    backgroundColor: 'transparent',
    boxShadow: 'none',
    position: 'relative',
    left: 'auto',
    right: 'auto',
    top: 'auto',
  },
});
