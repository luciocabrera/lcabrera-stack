# Pin/unpin from header now uses the same shared order-sync utility as drawer flows.

# Unpin from header repositions the column adjacent to the remaining pinned group

# (reorder-to-fill) so table viewport and settings drawer stay in sync.

# Header pinning still uses PinSideModal / conflict handling for side selection.
