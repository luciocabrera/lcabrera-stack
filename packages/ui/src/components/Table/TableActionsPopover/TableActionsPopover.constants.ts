/** Vertical gap between the trigger and the opened menu. */
export const MENU_GAP_PX = 4;

/** Horizontal offset applied after right-aligning the menu to its anchor. */
export const MENU_HORIZONTAL_NUDGE_PX = 2;

/** Frames to keep repositioning after open while table geometry settles. */
export const MENU_REPOSITION_FRAMES = 10;

/** Minimum distance kept between the menu and the container edges. */
export const MENU_VIEWPORT_PADDING_PX = 8;
