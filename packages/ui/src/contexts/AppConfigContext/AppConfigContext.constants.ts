/**
 * Where the navigation's session controls submit when an app does not override
 * it. A published package cannot assume an app's route table, so this is a
 * default rather than a constant the components read directly.
 */
export const DEFAULT_LOGOUT_ROUTE = '/logout';
