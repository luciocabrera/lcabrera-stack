---
name: fixture-skill
description: Test fixture only — not a real skill, used by skillFrontmatter.util.test.ts.
allowed-tools: Bash(cat:*,git:*), Read, Grep
---

# Fixture Skill

Body content used to assert `loadSkillFrontmatter` returns the text after the
closing frontmatter fence.
