# scan-ingestion Architecture

CQMS's entire persistence/API backend (TECH_SPEC §2.4 decision #4) — no
route or job anywhere reaches `apps/api-server`. A thin validate-then-call
library: Zod-parses a scanner's `report.json`/`report.md` off disk, resolves
or creates the run/scan/project rows, and delegates the actual multi-table
write to `cqms.sp_ingest_scan_result` (see ADR-006). Two consumers: each
skill's final step via the CLI wrapper, and `admin_system`'s background job
importing `ingestReport` directly (`workspace:*`, no subprocess).

## File Structure

```
scan-ingestion/
├── ARCHITECTURE.md
├── package.json          → format/lint/typecheck/migrate scripts (no "test" — vite-plus naming conflict, see ADR-004)
├── vite.config.ts         → fmt + api-lint + test task, self-referencing @repo/scan-ingestion alias
├── tsconfig.json           → ADR-003 wrapper ({ files: [], references: [...] })
├── tsconfig.app.json        → generated by packages/ts-configs/generate.ts (createNodeTsConfig)
│
├── src/
│   ├── db/
│   │   ├── migrations/          → 0001_init_cqms.sql .. 0004_seed_scanners.sql (ADR-006)
│   │   ├── env.schema.ts         → Zod-validated DB_HOST/PORT/USER/PASSWORD/NAME
│   │   ├── getPool.util.ts       → Lazy singleton pg.Pool + closePool (library, no single entry point)
│   │   └── runMigrations.ts      → Minimal runner (ADR-006) — plain node script, relative imports
│   │
│   ├── ingestion/
│   │   ├── report.schema.ts             → The report.json contract (Zod) — ScanFinding, RunFileInput, Report
│   │   ├── ingestReport.ts               → The core function
│   │   ├── ingestReport.types.ts         → IngestReportArgs/Result
│   │   ├── resolveScan.util.ts           → UI path (lookup) vs ad hoc path (create project+run+scan)
│   │   ├── matchProject.util.ts          → git rev-parse --show-toplevel + realpath canonicalization
│   │   ├── classifyFileTypeCategory.util.ts → Suffix → category (component/hook/util/...)
│   │   └── buildFileInventory.util.ts    → Walks localPath → RunFileInput[] (whole-project scopes only)
│   │
│   └── cli/
│       └── ingest.cli.ts   → Thin argv wrapper each skill's final step invokes (relative imports — see below)
```

## Two Execution Contexts, One Package

This package has consumers in **two genuinely different JS runtimes**, and
the import style deliberately differs between them:

- **Vite/Vitest/tsc context** (tests, and any _other_ package importing this
  one): resolves `@repo/scan-ingestion/*` via `tsconfig.app.json`'s `paths`
  and `vite.config.ts`'s `resolve.alias` — same self-referencing-alias
  convention `packages/ui` already established, per
  `.claude/rules/typescript.md`'s "relative imports only within the same
  directory" rule.
- **Plain `node --experimental-strip-types` context** (`db/runMigrations.ts`,
  `cli/ingest.cli.ts`, and everything they transitively import): neither
  tsconfig `paths` nor Vite's `resolve.alias` apply here — there is no
  bundler or tsc in the loop, only Node's own ESM resolver, which only
  understands relative/absolute specifiers and real installed packages.

Concretely: `ingestReport.ts` is loaded by **both** contexts (Vitest tests,
_and_ transitively via `ingest.cli.ts`), so it — and everything on that
transitive path (`getPool.util.ts`) — uses **relative** imports. Only
`ingestReport.test.ts` (Vitest-only, never bare-node) safely uses the
`@repo/scan-ingestion/*` alias. Getting this backwards produces a working
`tsc`/`vitest` run that then fails at real CLI runtime with
`ERR_MODULE_NOT_FOUND` — caught empirically by actually running the CLI, not
just typechecking it.

## The `report.json` Contract

No `report.json` exists anywhere yet as of this package's build — `SCHEMA_V1.md`
(`.github/skills/code-smell-shared/`) defines a **Markdown** report contract
only. `report.schema.ts` is this package _defining_ the JSON contract Step 5
retrofits the three existing skills to emit, modeled on `cqms.scan_findings`'
actual `NOT NULL`/`CHECK` constraints (TECH_SPEC §2.3a: "not a field-by-field
mirror... Postgres owns per-field validity"), not a literal transcription of
`SCHEMA_V1.md`'s prose "required" list.

Two things this shape must get right, both discovered before writing any
skill-facing code:

1. **`status: "resolved"` is real and already in production `report.md`
   files** (both `code-smell-checker` and `fallow-code-checker`), but neither
   `SCHEMA_V1.md` nor `cqms.scan_findings`' `CHECK` constraint accepts it —
   only `open|in-progress|done|deferred`. `scanFindingSchema` rejects
   `resolved` deliberately; Step 5 must map it to `done` when generating
   `report.json`, not this schema silently coercing it.
2. **`verification_steps`/`extra`/`finding_kind` need real defaults applied
   before `ingestReport` ever calls `sp_ingest_scan_result`.** The procedure
   bulk-inserts findings via `jsonb_to_recordset`, which does **not** apply a
   column's `DEFAULT` for an absent or `null` JSON key — it inserts SQL
   `NULL`. Those three columns are `NOT NULL DEFAULT ...`, so an omitted key
   would fail the constraint instead of silently getting the default.
   `scanFindingSchema`'s `.default([])`/`.default({})`/`.default('single_location')`
   is what keeps the bulk insert correct regardless of what a scanner's
   emission logic omits.

`reportSchema`'s top-level shape (`blocker_count`/`high_count`/... flat, not
`SCHEMA_V1.md`'s nested `findings_count_by_severity`) is deliberately shaped
to match `sp_ingest_scan_result`'s `p_report_metadata` parameter almost
verbatim, so `ingestReport` needs no reshaping step in between — staying a
genuinely thin wrapper.

## `ingestReport()` Flow

1. Zod-parse `reportJsonPath` (thin top-level check — `reportSchema.parse`).
2. Read `reportMarkdownPath` (verbatim) and `rawJsonPath` if given (only
   `fallow` populates this today).
3. `resolveScan` — **UI path** (`args.runId` given): look up the existing
   run's `project_id` and find the matching `(run_id, scanner_id)` scan row
   (already created by the trigger-scan action before the job started).
   **Ad hoc path** (no `runId`): `resolveProjectPath` (git-root + realpath),
   `fn_upsert_project`, `fn_create_run`, then insert the `scans` row directly.
4. If `rawJson`/`health_metrics` present, one `UPDATE cqms.scans` (these
   aren't `sp_ingest_scan_result` parameters — they're set directly).
5. `buildFileInventory` only for whole-project scopes (`repo`/`folder`) —
   `null` for `diff`-scoped scans (`code-smell-zen`).
6. `CALL cqms.sp_ingest_scan_result(...)` — the one multi-table write, done
   entirely in Postgres (TECH_SPEC §2.3a), not hand-assembled in TypeScript.

## Verification

Every layer verified against the real, live `cqms_db` (ADR-006's schema) —
no mocked `pg` client anywhere: `ingestReport.test.ts` exercises both the ad
hoc and UI paths end-to-end (project/run/scan creation, finding insertion,
run-status finalization, then cleanup), and the CLI wrapper itself was run
directly (`node --experimental-strip-types src/cli/ingest.cli.ts ...`)
against a real temporary git repo, with the resulting Postgres rows queried
directly to confirm correctness before cleanup.
