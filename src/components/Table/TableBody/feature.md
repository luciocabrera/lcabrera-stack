let's implement a feature to show the enterprise order details by its id/key.

- add the action column to the /home/lucio/workspace/frameworks/vite-react-compiler/src/routes/enterprise-orders/EnterpriseOrders.constants.tsx
- it must have a button/link whihc will navigate to a nested route /enterprise-orders/id
- use the react router 7 nested routing approach
- we must implement an endpoint to get all the dfetails in the /home/lucio/workspace/frameworks/vite-react-compiler/api-server
- the nested router will show all the details in a nice design
- it must have a button to close the details and that would navigate back
- reuse as much as possible