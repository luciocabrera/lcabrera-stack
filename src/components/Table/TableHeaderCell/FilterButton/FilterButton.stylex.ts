import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  activeIcon: {
    color: 'var(--color-primary)',
  },
  button: {
    padding: '0.25rem',
    minHeight: 'unset',
    minWidth: 'unset',
  },
  icon: {
    // Base icon styles
  },
});
