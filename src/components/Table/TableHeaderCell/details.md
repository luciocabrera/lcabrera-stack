# the columns pinning implemented in the table settings drawer and column settings drawer make sure that the order gets updated accordingly.

 # now, when the user clicks in the Button in the TableHeaderCell.component.tsx is pinning but we also need to update the column order



 # the columns pinning implemented in the table settings drawer prompts the user with a modal to decide which side to pin the column

 # we should prompt the user with the same modal when pining from the TableHeaderCell.component.tsx

