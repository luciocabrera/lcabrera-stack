import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  selectOverride: {
    borderRadius: 0,
    borderStyle: 'none',
    overflow: 'visible',
    backgroundColor: 'transparent',
  },
});

