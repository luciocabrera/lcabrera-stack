/** Height of each option row in pixels */
export const ITEM_HEIGHT = 32;

/** Default max-height for the scrollable list container */
export const LIST_MAX_HEIGHT = '18.75rem';

/** Fallback container height (px) used when the ref is not yet measured */
export const DEFAULT_CONTAINER_HEIGHT = 300;

/** Distance from the bottom (px) at which infinite-scroll triggers a fetch */
export const SCROLL_THRESHOLD = 50;
