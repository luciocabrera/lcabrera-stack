# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage/
.tmp/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
node_modules/
docker/pgpass
docker/local/pgpass

# Env files are local-only, anywhere in the tree — only `.env.example`
# templates are tracked (AGENTS.md § Security: never commit .env files or
# credentials). These patterns are deliberately path-less so they cover every
# workspace: the previous per-path list left the repo root and most of
# packages/* uncovered, and `apps/api-server/.env` still reached git carrying
# a DB_PASSWORD.
.env
.env.*
!.env.example

packages/eslint-local-rules/build/

# Fallow per-run artifacts (canonical root: reports/fallow/ — see AGENTS.md)
reports/fallow/runs/
