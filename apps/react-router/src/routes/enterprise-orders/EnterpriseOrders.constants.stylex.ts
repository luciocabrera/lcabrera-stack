import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  actionsCellContent: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    width: '100%',
  },
});
