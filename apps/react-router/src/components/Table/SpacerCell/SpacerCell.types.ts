export type SpacerCellProps = {
  /** Whether to render as a <th> (true) or <td> (false, default). */
  readonly isHeader?: boolean;
  /** Width of the spacer in pixels. */
  readonly width: number;
};
